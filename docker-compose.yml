services:
  gateway:
    build:
      context: .
    environment:
      MCP_SHIELD_HTTP_ADDR: 0.0.0.0:8080
    ports:
      - "8080:8080"
    read_only: true
    security_opt:
      - no-new-privileges:true